{
  "name": "min-radio",
  "version": "1.3.0",
  "private": true,
  "type": "module",
  "description": "Personal Sveriges Radio PWA - fully static, deployable to GitHub Pages",
  "scripts": {
    "start": "node server.js",
    "build": "node --check app.js && node --check src/episode-seek.mjs && node --check sw.js && node --check tests/fixpass.test.mjs && node scripts/build-pages.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
